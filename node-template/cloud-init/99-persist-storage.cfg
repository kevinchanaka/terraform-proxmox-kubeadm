#cloud-config
# Runs on first boot of every clone — mounts persistent etcd/PKI disk if present.
# The || true makes it a no-op on nodes without the persistent disk attached.
runcmd:
  - /usr/local/sbin/setup-persistent-storage.sh /dev/sdb || true
